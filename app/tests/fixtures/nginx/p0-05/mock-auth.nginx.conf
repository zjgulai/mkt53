worker_processes 1;

events {
    worker_connections 64;
}

http {
    access_log off;

    server {
        listen 80;

        location = /portal/auth {
            if ($http_cookie ~* "portal_session=valid") {
                return 204;
            }

            return 401;
        }
    }
}
