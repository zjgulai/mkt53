worker_processes 1;

events {
    worker_connections 64;
}

http {
    access_log off;

    upstream portal_auth {
        server portal_auth:80;
    }

    include /etc/nginx/conf.d/mkt53-portal-gate.candidate.conf;
}
